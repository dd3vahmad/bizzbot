const Chat = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      This is the default chat landing page with the chat interface.
    </div>
  );
};

export default Chat;
